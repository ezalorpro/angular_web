import { PokemonTypesDirective } from './pokemon-types.directive';

describe('PokemonTypesDirective', () => {
  it('should create an instance', () => {
    const directive = new PokemonTypesDirective();
    expect(directive).toBeTruthy();
  });
});
